import type { FastifyInstance } from "fastify";

/**
 * Charge route handlers — stub registration for T-020.
 * Full route implementations are added in T-025.
 */
export async function chargeRoutes(_fastify: FastifyInstance): Promise<void> {
  // Route handlers registered in T-025
}
